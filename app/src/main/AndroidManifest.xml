<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.bvv.pharmplus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".MapActivity" />
        <activity android:name=".catalog.medicines.AkusherstvoGynecologyInfectionActivity" />
        <activity
            android:name=".ShoppingCartActivity"
            android:label="@string/shopping_cart"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".catalog.medicines.MedicinesActivity" />
        <activity android:name=".catalog.medicines.AkusherstvoGynecologyInfertilityActivity" />
        <activity android:name=".catalog.medicines.AkusherstvoGynecologyActivity" />
        <activity android:name=".catalog.MedGoodsActivity" />
        <activity android:name=".catalog.LensesActivity" />
        <activity android:name=".catalog.HygieneActivity" />
        <activity android:name=".catalog.VitaminsActivity" />
        <activity android:name=".catalog.CatalogMedicinesActivity" />
        <activity android:name=".CatalogActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>